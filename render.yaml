services:
  - type: web
    name: portfolio-site
    runtime: static
    buildCommand: npm install && npm run build
    publishPath: dist
    regions: oregon
    envVars:
      - key: NODE_VERSION
        value: 20
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
